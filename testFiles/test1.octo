pipeline main(one: vec4, two: vec4, three: vec4) -> (vec4)
    with {
        camera_pos: vec3,
        light_color: vec3,
        light_pos: vec3,
        roman_pos: vec2,
        wutss: mat4,
        wotc1: mat3,
    }
{
    let p = 12.0;
    let ret = one * p;
    let return_val = one;
    let tmp = one == one;
    let intoo = camera_pos.x;
    let somethin = vec2(1.0, 2.0);
    let tmp2 = sin(one);


    let sth14 = vec4(1.0, 0.0, 1.0, 1.0);
    let some_vec3 = wutss * sth14;

    sth14.x = one.x;
    sth14.xy = one.yz;
    sth14.z = camera_pos.x;

    return sth14;
}
