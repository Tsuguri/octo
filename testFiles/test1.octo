pipeline main(one: vec4) -> (Vec4){
    let z2 = 2.0;
    for(let p =0; p < 10 == z2 <2.0; p= p+1) {
        let g = 12;
    }
    let g = 2.0 + 3.0;
    let le = 2.0 - 3.0;
    //let w = shift(g, 3);
    let rum = g - le;
    let z = g / 1.5;
    z = z * 2.0;
    let dwa = one;
    let trzy = dwa + one;

    //let f = 2.0;
    //g = f / 2.0 + 3.10 + 13.0;// lolz("heh", wut);
    return trzy;

}
