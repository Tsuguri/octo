pipeline main(one: vec4, two: vec4, three: vec4) -> (vec4){
    let p = one + two;
    let some = one == three;
    let tmp = one == two;
    let tmp2 = one != two;

    for(let i = 0; i<5; ){
        tmp2 = !tmp2;
    }
    
    let done = tmp;
    return p + three;
}
